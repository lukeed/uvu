{
  "private": true,
  "type": "module",
  "scripts": {
    "start": "sirv src --dev",
    "test": "uvu tests -i setup"
  },
  "devDependencies": {
    "puppeteer": "^5.5.0",
    "sirv-cli": "^1.0.6",
    "uvu": "^0.5.0"
  }
}
